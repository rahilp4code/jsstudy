// HTML has auto correction which closes the tag correctly if u forgot to close and adds data in tags if they are empty eg. <tbody> tag in <table> tag

//Page Body tag
document.body

//page HTML tag
document.documentElement // `e` of element should be capital

//page head tag
document.head

//changing the title
document.title = `hlo ninjas`
document.title //returns string and its type is string unlike upper codes which return object and are object data type (head.body.documentElement)
